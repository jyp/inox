This directory contains sources for the [[http://bnfc.digitalgrammars.com/][~bnfc~]] tool. It is used to
easily exclude files generated by ~bnfc~ from git.

/Any files without the ~.cf~ extension will be ignored by git/
